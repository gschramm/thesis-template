%---- Page geometry -----------------------------------------------------
\usepackage[
  paper=b5paper,
  inner=30mm, outer=22mm,
  top=25mm, bottom=25mm,
  headheight=14pt, headsep=8mm
]{geometry}

%---- Fonts (pdfLaTeX) --------------------------------------------------
\usepackage{libertinus}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

%---- Spacing -----------------------------------------------------------
\usepackage{setspace}
\setstretch{1.15}
\setlength{\parindent}{1.2em}
\widowpenalty=10000   % forbid widows (lone line at top of page)
\clubpenalty=10000    % forbid orphans (lone line at bottom of page)

%---- Colors ------------------------------------------------------------
\usepackage{xcolor}
\definecolor{thesisblue}{RGB}{0, 51, 102}   % signature dark-navy blue
\definecolor{headergray}{gray}{0.50}         % running-head tint

%---- Mathematics -------------------------------------------------------
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{siunitx}

%---- General packages --------------------------------------------------
\usepackage{graphicx}
\usepackage[labelfont=bf,font=footnotesize]{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{microtype}
\usepackage[
  hidelinks,
  pdftitle  = \ThesisTitle,
  pdfauthor = \ThesisAuthor,
]{hyperref}
\usepackage[titletoc]{appendix}
\usepackage{lipsum}
\usepackage{csquotes}    % context-sensitive quotation marks via \enquote{}
\usepackage{longtable}    % tables that span multiple pages
\usepackage{xltabular}   % longtable + tabularx X columns combined
\usepackage{pdflscape}   % landscape pages for wide tables/figures

%---- Smart cross-references (must come after hyperref) -----------------
\usepackage[capitalise, noabbrev]{cleveref}

%---- Bibliography (per-chapter) ----------------------------------------
\usepackage[
  backend    = biber,
  style      = alphabetic,
  sorting    = anyt,
  refsection = chapter,
]{biblatex}
\addbibresource{ch1/ch1.bib}
\addbibresource{ch2/ch2.bib}
\addbibresource{ch3/ch3.bib}
\AtBeginBibliography{\footnotesize}

%---- Acronyms ----------------------------------------------------------
\usepackage{acro}
\DeclareAcronym{snr}{short = SNR,  long = signal-to-noise ratio}
\DeclareAcronym{qc} {short = QC,   long = quality control}
\DeclareAcronym{cdf}{short = CDF,  long = cumulative distribution function}
\DeclareAcronym{roc}{short = ROC,  long = receiver operating characteristic}
\DeclareAcronym{ml} {short = ML,   long = machine learning}

%---- Thumb index (thumbs package) --------------------------------------
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage[
  height=10mm,
  width=auto,
]{thumbs}

%%---- Chapter title format (titlesec) -----------------------------------
\titleformat{\chapter}[display]
  {\normalfont\raggedright}
  {\color{thesisblue}\sffamily\bfseries\fontsize{96pt}{96pt}\selectfont\thechapter}
  {6pt}
  {\Huge\bfseries\sffamily}

\titleformat{\section}{\bfseries\large\sffamily}{\thesection}{1em}{}
\titleformat{\subsection}{\bfseries\normalsize\sffamily}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\bfseries\normalsize\sffamily}{\thesubsubsection}{1em}{}
  

%%---- Headers & footers -------------------------------------------------

% Wrapped in \AtBeginDocument so this runs after all package \AtBeginDocument
% hooks (thumbs, biblatex, titlesec) and cannot be overridden by them.
\AtBeginDocument{%
   % Patch for pageslts bug: thumbs appear on wrong (inner) side
  % \markboth sets: leftmark  = "Chapter N"  (shown on left/even pages)
  %                 rightmark = chapter title (shown on right/odd pages)
  \renewcommand{\chaptermark}[1]{%
    \markboth{Chapter~\arabic{chapter}}{#1}%
    %\addthumb{#1}{\thechapter}{white}{thesisblue}%
  }%
  \renewcommand{\sectionmark}[1]{}% keep rightmark = chapter title, not section
  \pagestyle{fancy}%
  \fancyhf{}%
  \fancyhead[LE]{\small\textcolor{headergray}{\leftmark}}%   left-aligned on left pages
  \fancyhead[RO]{\small\textcolor{headergray}{\nouppercase{\rightmark}}}% right-aligned on right pages
  \fancyfoot[LE,RO]{\thepage}%                              page number on outer corner
}

%%---- TikZ and pgfplots ------------------------------------------------
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

%%---- Algorithms -------------------------------------------------------
\usepackage{algorithm}
\usepackage{algpseudocode}
%========================================================================
